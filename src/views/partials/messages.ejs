<% if (notice) { %>
  <div class="alert alert--success">
    <span><%= notice %></span>
  </div>
<% } %>
<% if (error) { %>
  <div class="alert alert--danger">
    <span><%= error %></span>
  </div>
<% } %>
